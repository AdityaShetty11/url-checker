# URL Checker

A browser app that checks if a URL exists and whether it points to a file or folder.

## Run

```bash
cd path/to/project
python3 -m http.server 8080
```

Then open `http://localhost:8080` in your browser.